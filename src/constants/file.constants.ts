import { join } from 'path';

export const INPUT_DIRECTORY_PATH = join(process.cwd(), 'test-maps');
export const OUTPUT_DIRECTORY_PATH = join(process.cwd(), 'generated');

export const FILENAME = 'test-map-2.txt';
